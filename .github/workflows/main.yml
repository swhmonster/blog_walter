# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: master

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@master

      # Runs a single command using the runners shell
#       - name: Run a one-line script
#         run: echo Hello, world!

      # Runs a set of commands using the runners shell
      # Deploy
      - name: Deploy to Server
        uses: easingthemes/ssh-deploy@v2.0.8
        env:
          ARGS: "-avz --delete"
          REMOTE_HOST: 49.234.97.199
          REMOTE_USER: root
          REMOTE_PORT: 22
          SOURCE: "dist/"
          TARGET: "/home/blog_walter/"

#       - name: Run a multi-line script
#         run: |
#           echo "=============== 开始部署任务-DEVELOP  ==============="
#           sshpass -p *** ssh -o StrictHostKeychecking=no root@49.234.97.199
#           echo "=============== 将程序包包部署到远程服务器上  ==============="
#           sshpass -p *** scp -o StrictHostKeychecking=no ./vuepress-starter/docs/.vuepress/dist/ root@49.234.97.199:/home/blog_walter
          
